import { CircularProgress } from "@mui/material";
import "./Loader.css";

const Loader = () => {
  return (
    <div className="Loader">
      <CircularProgress />
    </div>
  );
};

export default Loader;